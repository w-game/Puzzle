namespace ByteDance.Union
{
    /// <summary>
    /// // 开屏点击区域类型
    /// </summary>
    public enum ABUDownloadType
    {
        DOWNLOAD_TYPE_NO_POPUP = 0,    //  不强制弹窗，默认值；
        DOWNLOAD_TYPE_POPUP = 1,   // 调起下载强制触发下载确认弹窗（不含商店直投场景）；
    }
}
